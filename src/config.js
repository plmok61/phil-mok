export const gridSize = 200;
export const gameInterval = 30;
export const baseSize = 3;

// Mouse-hover cell fill (semi-transparent aqua glow)
export const cursorColor = 'rgba(94, 234, 212, 0.6)';

// Background board (behind page content, below the hero)
export const bgCellSize = 8;
export const spawnDistance = 60;
// Fraction of the hero's height the user must scroll past before
// cursor movement starts spawning spaceships on the background board.
export const spawnScrollThreshold = 0.25;
